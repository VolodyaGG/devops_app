apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-service"
spec:
  selector:
    app: cbr-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP